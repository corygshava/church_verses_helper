settings
	make it to be like the rainmaster app
	the app is meant for changing styles for a page with certain elements each stored in a scene. each scene is just an object that holds elements in the format 
	{
		scenename,
		scenetemplate : `
		<div class="content versedisplay" data-role="[background]">
			<span class="verse-text" id="gottenverse" data-role="[text1name]">...</span>
			<span class="verse-location" id="output" data-role="[text2name]">Waiting for verse...</span>
		</div>` (common for all scenes for now but i want it that its autogenerated based on the contents of myelements),
		myelements: [{mytype: (either text or background),demotext: (default text to show when initialised)}],
		datecreated
	}.
	make it that theres an array of scenes called thescenes which is fetched via an API call to (/api_getscenes) (creates an array with the default scene if theres an error)
	make it that theres a layers panel on the left which shows all the elements in the selected scene stacked and a third element labelled (slide) and when one is clicked the element is selected.
	theres a canvas with the elements make it that theres a properties panel with all the necessary styles
		for text (text color, font weight: 200 - 900,text outline, text outline width, text size, text font)
		for the background (background color, background size (a select with the css options),background attachment (a select with the css options), background style)
			if the style is plain (background color)
			if the style is gradient (gradient type (linear, radial, conical), number of colors (this increases the number of color inputs to be entered))
			if the style is image (image src, upload image (this uploads an image to the server via an api call to (/api_uploadimg) which uploads the image and returns the image's new path which sets the image src), pick image (this shows a selection of uploaded images and images currently on the server (fetched via an api call to (/api_getimages)) when each image is clicked it sets the image src to be that of the chosen image)) also make it that the background image is updated in realtime as the image src is modified and an event fires as soon as the image is loaded.
		fot the slide (entrance animation (a select of 3 predefined animations) and fields for entering its duration and easing,exit animation (also a select of 3 predefined animations)  and fields for entering its duration and easing)
	make it to have a button called 'send' payload which will send a json payload via post to (/api_stylesmanager) in the format 
		{
			scene: scenename,
			txtstyles : {txt_(the property from first till last)},
			bgstyles : {bg_style,bgdata: bg_d_(the property from first till last entered depending on the context via background style)},
			slidestyles: {slide_(the property from first till last)}
		}
		and a message to a websocket endpoint
	make it that theres a toolbar at the top which has a div on the left with the name of the app (worship helper PRO) and application actions (make them, but i need for there to be a way to switch the currently active scene and a way to import a json file of a scene's settings) and a separate div with a button on the far right which is a light mode / dark mode toggle.
	make it that in the canvas it shows those 3 elements. given i want the html structure of whats shown to be in the form {
		<div class="content versedisplay">
			<!-- <h1>Receiver Output:</h1> -->
			<span class="verse-text" id="gottenverse">...</span>
			<span class="verse-location" id="output">Waiting for verse...</span>
		</div>
	} so that i can edit it using whatever CSS i need.

follow up
	great now make it into a single page html app. consider that the api endpoint returns data in the form {success: true,echo: (json result)} and on failure sends back {success: false,echo: 'Invalid endpoint'} also note that you should attach an external js at js/app.js with a variable socket for the wss connection and a function sendMessage(msg) for sending a message to the wss server. also make it use segoe ui as a fontface

go crazy
	now just go nuts and make it into a app with as much functionality as your compute can allow. feel free to build off of what i made. also make it have a darkmode look and one of those dark with a subtle gradient styling. make it that it also shows a modal with a short tutorial on how to use and that is also has a small panel at the bottom showing runtime details (current scene, number of items etc). add more features if you want. now go